<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="description" content="Play On Pricing Tool - Video game resale pricing for fair prices and fast inventory turnover">
  <meta name="theme-color" content="#1A365D">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  
  <title><%= title %></title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="manifest" href="/manifest.json">
  <link rel="icon" type="image/svg+xml" href="/images/logo.svg">
  <link rel="apple-touch-icon" href="/images/icon-192.png">
</head>
<body>
  <div class="page">
    <header class="header">
      <div class="container">
        <div class="header-content">
          <a href="/" class="logo">
            <svg class="logo-icon" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
              <circle cx="20" cy="20" r="18" stroke="#D69E2E" stroke-width="3" fill="none"/>
              <path d="M16 12L28 20L16 28V12Z" fill="#D69E2E"/>
            </svg>
            <div>
              <span class="logo-text"><span class="play">PLAY</span> <span class="on">ON</span></span>
              <div class="tagline"><%= tagline %></div>
            </div>
          </a>
          <nav class="nav">
            <a href="/" class="nav-link active">Price</a>
            <a href="/calculator" class="nav-link">ROI</a>
            <a href="/session" class="nav-link">Session</a>
            <a href="/platforms" class="nav-link">Codes</a>
          </nav>
        </div>
      </div>
    </header>
    
    <main class="main-content">
      <div class="container">
        <div class="card">
          <div class="card-header">
            <h1 class="card-title">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
              Price Lookup
            </h1>
          </div>
          
          <form id="searchForm" class="search-box">
            <div class="search-input-wrapper">
              <svg class="search-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
              <input type="text" id="searchInput" class="search-input" placeholder="Enter game title or UPC..." autocomplete="off" autocapitalize="off" spellcheck="false">
            </div>
            
            <select id="platformSelect" class="platform-select">
              <option value="">All Platforms</option>
              <optgroup label="Current Gen">
                <% platforms.filter(p => p.gen === 'current').forEach(p => { %>
                <option value="<%= p.code %>"><%= p.name %></option>
                <% }) %>
              </optgroup>
              <optgroup label="Previous Gen">
                <% platforms.filter(p => p.gen === 'previous').forEach(p => { %>
                <option value="<%= p.code %>"><%= p.name %></option>
                <% }) %>
              </optgroup>
              <optgroup label="Retro">
                <% platforms.filter(p => p.gen === 'retro').forEach(p => { %>
                <option value="<%= p.code %>"><%= p.name %></option>
                <% }) %>
              </optgroup>
              <optgroup label="Handheld">
                <% platforms.filter(p => p.gen === 'handheld').forEach(p => { %>
                <option value="<%= p.code %>"><%= p.name %></option>
                <% }) %>
              </optgroup>
            </select>
            
            <button type="submit" class="btn btn-primary">Search</button>
          </form>
        </div>
        
        <div id="priceResults">
          <div class="empty-state">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <p>Search for a game to see pricing data</p>
            <p class="text-muted">Uses PriceCharting & eBay sold listings</p>
          </div>
        </div>
        
        <div class="card">
          <div class="card-header">
            <h2 class="card-title">Quick Reference</h2>
          </div>
          <div class="price-grid">
            <div class="price-item">
              <div class="price-label">Sell Price</div>
              <div class="price-value">85-90%</div>
              <div class="text-muted" style="font-size: 0.75rem;">of market value</div>
            </div>
            <div class="price-item">
              <div class="price-label">Max Buy</div>
              <div class="price-value gold">50%</div>
              <div class="text-muted" style="font-size: 0.75rem;">of market value</div>
            </div>
            <div class="price-item">
              <div class="price-label">Min Profit</div>
              <div class="price-value highlight">$5+</div>
              <div class="text-muted" style="font-size: 0.75rem;">per item</div>
            </div>
            <div class="price-item">
              <div class="price-label">Target ROI</div>
              <div class="price-value">70%+</div>
              <div class="text-muted" style="font-size: 0.75rem;">ideal</div>
            </div>
          </div>
        </div>
      </div>
    </main>
    
    <footer class="footer">
      <div class="container">
        <span class="footer-brand">▶ PLAY ON</span> &bull; Fair Prices. Sincere Service. Quality Finds.
      </div>
    </footer>
  </div>
  <script src="/js/app.js"></script>
</body>
</html>
